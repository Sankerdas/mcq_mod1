import { AnsLinkDirDirective } from './ans-link-dir.directive';

describe('AnsLinkDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AnsLinkDirDirective();
    expect(directive).toBeTruthy();
  });
});
